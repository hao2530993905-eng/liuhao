# EpicEye SDK for Python

## 使用方法
#### 安装依赖
```shell
pip install -r requirements.txt
```

#### 运行示例程序

手动指定相机运行：

假设ip为127.0.0.1#192.168.33.66
```shell
python ring.py 192.168.33.66
或者不指定，直接运行
python ring.py 1
```

手动指定图片运行：
```shell
python ring.py
```

tips:内圈识别不到一般光照原因



